# Docker Compose — PostgreSQL
DB_USER=ocr_user
DB_PASSWORD=change_me
DB_NAME=ocr_db

# Docker Compose — Qdrant
QDRANT_API_KEY=change_me

# Backend — Redis (BullMQ queue connection)
REDIS_HOST=localhost
REDIS_PORT=6379
